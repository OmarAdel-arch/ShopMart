export interface SubcategoryI {
    _id: string
    name: string
    slug: string
    category: string
}

export interface CategoryI {
    _id: string
    name: string
    slug: string
    image: string
}
export interface categoriesI {
    _id: string
    name: string
    slug: string
    image: string
    createdAt: string
    updatedAt: string
}
